{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Build Extension (ZIP)",
      "type": "shell",
      "command": "VERSION=$(grep '^version' blender_manifest.toml | cut -d'\"' -f2) && ID=$(grep '^id' blender_manifest.toml | cut -d'\"' -f2) && mkdir -p dist && zip -r \"dist/${ID}-${VERSION}.zip\" . -x '*.git*' '*__pycache__*' '*.vscode*' 'dist/*' 'docs/*'",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "presentation": {
        "echo": true,
        "reveal": "always",
        "focus": false,
        "panel": "shared",
        "showReuseMessage": false
      },
      "problemMatcher": []
    }
  ]
}